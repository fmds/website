

.content
    br
    ul.speakers
        for item in schedule
            li.speaker(class=item.type)
                    //div.time=item.time
                    img(src='').speaker
                    .detail
                        h4.title=item.title
                        p=item.desc
                        if item.speakers
                            each speak in item.speakers
                                - console.log(speak);
                                - speaker = allSpeakers[speak]
                            //- if(speaker)
                            //-     console.log(speak, allSpeakers) ;
                            //-     != partial("_speaker", speaker)


    p Got someone who needs to be on the list? Get in <a href="./contact.html">contact</a>.

    p We will be announcing new speakers in the coming weeks and months.
